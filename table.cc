#include "table.h"
#include <vector>

Table::Table() {
	
}

bool Table::add_players(unsigned int new_players) {
	new_players += players.size();
	for (int i(players.size()); i < )
}

bool Table::add_bots(unsigned int new_bots) {
	
}
